{ config, pkgs, ... }:
{
  # Home Manager user config (later):
  # - shell + prompt
  # - terminal (kitty/foot) + multiplexer (zellij/tmux)
  # - Firefox policies/extensions
  # - Neovim (via nixvim/nvf) with LazyVim
}
